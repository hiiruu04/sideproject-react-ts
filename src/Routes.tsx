import React from "react";
import { Switch, Route, Redirect } from "react-router-dom";

const BaseRouter = () => (
    <div>
        <Switch>
        </Switch>
    </div>
)

export default BaseRouter;